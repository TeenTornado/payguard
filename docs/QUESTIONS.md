# QUESTIONS.md

Things only the user can answer. Add questions here and continue on the emulator path.

## Open

1. **Razorpay test credentials** — Do you have `rzp_test_` key_id, key_secret, and webhook_secret available? If yes, FORWARD_TEST mode can be enabled for Phase 3+. If not, EMULATE mode only (fully functional for evaluation).

2. **Razorpay minimum order amount** — Brief says 100 paise `[verify]`. Please confirm or correct.

3. **Python SDK base_url override** — Need to verify whether `razorpay.Client(auth=..., base_url=...)` is the correct API. Will inspect installed SDK source during Phase 3.

## Resolved

_(none yet)_
