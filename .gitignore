.env
.env.local
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/
.venv/
venv/
node_modules/
.next/
.DS_Store
*.pkl
# LLM cache — subdirs are committed (demo-replay without live key)
# Only direct *.json files at the root are excluded (there are none)
!dataset/llm_cache/.gitkeep
# Legacy reports directory (pre-v2); new reports go to eval/reports/dev/ and eval/reports/test/
reports/
# but keep the FROZEN test-split eval reports (auditable freeze ledger). dev/ stays ignored.
!eval/reports/
eval/reports/dev/
*.log
docker-compose.override.yml

# web build artifacts
web/tsconfig.tsbuildinfo
web/next-env.d.ts

# sandbox target artifacts
examples/targets/*/node_modules/
examples/targets/*/.sandbox.log

# KB vector cache (rebuilt from dataset/kb_index/chunks.jsonl, which IS committed)
dataset/kb_index/chroma/
